- name: Install and start nginx
  hosts: frontend
  become: yes
  tasks: #list of tasks
    - name: Install nginx in any linux distribution 
      ansible.builtin.package:
        name: nginx
        state: present

    - name: Enable and start nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes